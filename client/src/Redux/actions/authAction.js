export const loginUser = () => async (dispatch) => {
    // api call 
    dispatch({
        type:"LOGIN"
        //payload :
    })
}
export const logoutUser = () => async (dispatch) => {
    // api call 
    dispatch({
        type: "LOGOUT"
        //payload :
    })
}